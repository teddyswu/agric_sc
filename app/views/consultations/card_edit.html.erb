<body>
  <%= render "shared/left_panel" %>
  <div id="right-panel" class="right-panel">
    <div class="breadcrumbs">
      <div class="col-sm-4">
        <div class="page-header float-left">
          <div class="page-title">
            <h1>Dashboard</h1>
          </div>
        </div>
      </div>
      <div class="col-sm-8">
        <div class="page-header float-right">
          <div class="page-title">
            <ol class="breadcrumb text-right">
              <li><a href="#">Dashboard</a></li>
              <li><a href="#">聊天內容</a></li>
              <li class="active">會員參數設定</li>
            </ol>
          </div>
        </div>
      </div>
    </div>
    <div class="content mt-3">
      <div class="row">
        <div class="col-lg-12">
          <div class="card">
            <div class="card-header"><strong>卡片編輯</strong></div>
              
              <div class="card-body card-block">
                <div class="alert alert-primary">
                  這邊是針對在諮詢完後，要顯示更多資訊的卡片樣式做編輯
                </div>
                <%= form_for @con, :url => card_update_consultation_path(@con), :method => :PATCH do |f| %>
                  <div class="form-group">
                    <%= f.label("標題", :class => 'form-control-label') %><%= f.text_field(:title, :class => "form-control", placeholder: "輸入標題")%>
                  </div>
                  <div class="form-group">
                    <%= f.label("圖片URL", :class => 'form-control-label') %><%= f.text_field(:pic, :class => "form-control", placeholder: "輸入圖片URL")%>
                  </div>
                  <div class="form-group">
                    <%= f.label(:cat, "描述", :class => 'form-control-label') %><%= f.text_field(:intro, :class => "form-control", placeholder: "輸入卡片描述")%>
                  </div>
                  <div class="form-group">
                    <%= f.label(:cat, "按鈕名稱", :class => 'form-control-label') %><%= f.text_field(:button_name, :class => "form-control", placeholder: "輸入開始按鈕名稱")%>
                  </div>
                  <div class="card-footer">
                    <button type="submit" class="btn btn-primary btn-sm">
                      <i class="fa fa-dot-circle-o"></i> Submit
                    </button>
                    <button type="reset" class="btn btn-danger btn-sm">
                      <i class="fa fa-ban"></i> Reset
                    </button>
                  </div>
                <% end %>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>