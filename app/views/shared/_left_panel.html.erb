<aside id="left-panel" class="left-panel">
  <nav class="navbar navbar-expand-sm navbar-default">
		<div class="navbar-header">
			<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#main-menu" aria-controls="main-menu" aria-expanded="false" aria-label="Toggle navigation">
        <i class="fa fa-bars"></i>
      </button>
      <a class="navbar-brand" href="./"><img src="/assets/sk2/img/ui/logo.png" style="height: 35px;" alt="Logo"></a>
      <a class="navbar-brand hidden" href="./"><img src="/assets/sk2/img/ui/logo.png" alt="Logo"></a>
    </div>
    <div id="main-menu" class="main-menu collapse navbar-collapse">
    	<ul class="nav navbar-nav">
        <li class="active">
        	<%= link_to " <i class='menu-icon fa fa-dashboard'></i>Dashboard ".html_safe, agris_path %>
        </li>
        <h3 class="menu-title"><%= I18n.t("word.chat_content") %></h3><!-- /.menu-title -->
        <% chat_contents = I18n.t("chat_content")  %>
        <% chat_contents.each_with_index do |cc, i|%>
          <li <%= "class='active'".html_safe if request.url.include?("#{cc[0]}") %>>
            <%= link_to " <i class='menu-icon fa fa-leaf'></i> #{cc[1]} ".html_safe,  "/#{cc[0]}" %>
          </li>
        <% end %>
        <h3 class="menu-title"><%= I18n.t("word.html_content") %></h3><!-- /.menu-title -->
        <% html_contents = I18n.t("html_content")  %>
        <% html_contents.each_with_index do |hc, i|%>
          <li <%= "class='active'".html_safe if request.url.include?("#{hc[0]}") %>>
            <%= link_to " <i class='menu-icon fa fa-leaf'></i> #{hc[1]} ".html_safe,  "/#{hc[0]}" %>
          </li>
        <% end %>
        <h3 class="menu-title"><%= I18n.t("word.friendly_farmer") %></h3><!-- /.menu-title -->
        <% friendly_farmers = I18n.t("friendly_farmer")  %>
        <% friendly_farmers.each_with_index do |ff, i|%>
          <li <%= "class='active'".html_safe if request.url.include?("#{ff[0]}") %>>
            <%= link_to " <i class='menu-icon fa fa-leaf'></i> #{ff[1]} ".html_safe,  "/#{ff[0]}" %>
          </li>
        <% end %>
        <h3 class="menu-title"></h3>
        <% others = I18n.t("other")  %>
        <% others.each_with_index do |o, i|%>
          <li <%= "class='active'".html_safe if request.url.include?("#{o[0]}") %>>
            <%= link_to " <i class='menu-icon fa fa-leaf'></i> #{o[1]} ".html_safe,  "/#{o[0]}" %>
          </li>
        <% end %>
        <li>
          <%= link_to(" <i class='menu-icon fa fa-building'></i>#{I18n.t('word.proposal_man')} ".html_safe, "#{YAML.load_file("config/customization.yml")[:campaign_domain]}/admin/campaigns", :target => "_blank") %>
        </li>
        <li>
        	<%= link_to(" <i class='menu-icon ti-shift-right'></i>登出 ".html_safe, destroy_user_session_path, :method => :delete) %>
        </li>
      </ui>
    </div>
  </nav>
</aside>